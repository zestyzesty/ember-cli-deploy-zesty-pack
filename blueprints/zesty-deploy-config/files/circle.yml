machine:
  node:
    version: 0.12.0

dependencies:
  post:
    - bower install

deployment:
  production:
    branch: master
    commands:
      - 'ember deploy production --activate'

  pull-request:
    branch: /^((?!master).)*$/
    commands:
      - 'if [ -n "$CI_PULL_REQUEST" ]; then ember deploy pull-request; fi'
